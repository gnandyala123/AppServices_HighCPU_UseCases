; PHP user INI — applies to all PHP requests in this directory
; Azure App Service picks this up automatically (both Windows and Linux stacks)

max_execution_time = 600
memory_limit = 256M
output_buffering = Off
implicit_flush = true
